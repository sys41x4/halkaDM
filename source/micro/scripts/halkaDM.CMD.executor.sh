cmdList=("echo Sleep_clked",
        "sudo shutdown -r now",
        "sudo /usr/sbin/shutdown -h now",
        "/usr/bin/cal",
        "/usr/bin/mpstat -P ALL",
        "/usr/sbin/tcpdump --list-interfaces",
        "ls /usr/share/xsessions | rev | cut -d '.' -f 2 | rev"
)
